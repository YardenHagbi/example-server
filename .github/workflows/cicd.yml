name: CICD / Build and Deploy to Azure

env:
  AZURE_WEBAPP_NAME: example-server
  AZURE_WEBAPP_PACKAGE_PATH: .
  NODE_VERSION: '20'

on:
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Echo branch name
        run: echo "Branch name: ${GITHUB_REF}"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: contains('refs/heads/dev,refs/heads/main', github.ref)
    env:
      SLOT: ${{ github.ref == 'refs/heads/main' && 'Production' || 'Dev' }}
      AZURE_CREDENTIALS: ${{ github.ref == 'refs/heads/main' && secrets.AZURE_CREDENTIALS_PROD || secrets.AZURE_CREDENTIALS_DEV }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          slot-name: ${{ env.SLOT }}
          package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
          publish-profile: ${{ env.AZURE_CREDENTIALS }}
